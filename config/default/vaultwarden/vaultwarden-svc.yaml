---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: vaultwarden.local.tun0.eu
  labels:
    app: vaultwarden
  name: vaultwarden
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: vaultwarden
  type: LoadBalancer
  loadBalancerIP: 192.168.178.206
  externalTrafficPolicy: Local
