---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: jackett.local.tun0.eu
  labels:
    app: jackett
  name: jackett
  namespace: default
spec:
  ports:
  - name: http
    port: 9117
    protocol: TCP
    targetPort: 9117
  selector:
    app: jackett
  type: LoadBalancer
  externalTrafficPolicy: Local
